package mapper

import (
	"github.com/petersunbag/coven"
)

var options = map[string]*coven.StructOption{}

func init() {
	// key1 := fmt.Sprintf("%v_%v", reflect.TypeOf(dtos.CustomField{}).String(), reflect.TypeOf(models.CustomField{}).String())

	// options[key1] = &coven.StructOption{}
}
